 @* @model List<SimpleCrm.VM.GetMyDailyTasksVM> *@ 
@{
    ViewData["Title"] = "User Tasks";
}
 <h1 class="text-center mt-5">User Tasks</h1>
<div class="row">
    <div class="row mb-3">
        <div class="col-12">
            <select id="statusFilter" class="form-select" style="width: 200px; margin: 0 auto;">
                <option value="All">All</option>
                <option value="1">Pending</option>
                <option value="2">In Progress</option>
                <option value="3" selected>Under Review</option>
                <option value="4">Completed</option>
            </select>
        </div>
    </div>
    <div class="row" id="tasksContainer"> 
        </div>
   @*   @foreach (var item in Model)
    {
            <div class="col-sm-12 col-md-3 mb-2 task-card" data-status="@item.Status">
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <p class="d-none">@item.Id</p>
                    <h5 class="card-title text-center"> @item.Title</h5>
                    <p class="card-text">@item.Description</p>
                    <p class="card-text">
                        DeadLine: @item.EndDate

                        @if (item.Status == "Pending")
                        {
                            <span class="text-info"> @item.Status</span>
                        }
                        @if (item.Status == "InProgress")
                        {
                            <span class="text-primary"> @item.Status</span>
                        }
                        @if (item.Status == "UnderReview")
                        {
                            <span class="text-success"> @item.Status</span>
                        }
                        @if (item.Status == "Completed")
                        {
                            <span class="text-primary"> @item.Status</span>
                        }

                    </p>
                    @if (item.Status == "UnderReview")
                    {
                        <button onclick="CompleteTask('@item.Id')" class="btn btn-primary" type="button"><i class="bi bi-check-circle"></i> Complete</button>
                    }
                </div>
            </div>
        </div>
    }   *@
    
</div>
 
@section Scripts{
    <script>

        $(document).ready(function () {
            const selectedStatus = $(this).val();
            const urlParams = new URLSearchParams(window.location.search);
            const userId = urlParams.get('UserId');
            GetTasks(userId, 3);
            // Handle change event on the dropdown
            $('#statusFilter').on('change', function () {
                 const selectedStatus = $(this).val();
                 const urlParams = new URLSearchParams(window.location.search);
                const userId = urlParams.get('UserId');
                 GetTasks(userId,selectedStatus);

            });

        });
         function GetTasks(userId,selectedStatus) {
                $.ajax({
                url: `/api/Values/Sdf?UserId=${userId}&Status=${selectedStatus}`,
                type: 'get',
                 success: function (data) {
                // Check if `data` needs parsing or has a specific property
                let tasks = Array.isArray(data) ? data : JSON.parse(data);

                const tasksContainer = $('#tasksContainer');
                tasksContainer.empty();
                     console.log(tasks);
                if (tasks.length > 0) {
                    tasks.forEach(task => {
                        const taskCard = `
                        <div class="col-sm-12 col-md-3 mb-2">
                            <div class="card" style="width: 18rem;">
                                <div class="card-body">
                                    <h5 class="card-title text-center">${task.title}</h5>
                                    <p class="card-text">${task.description}</p>
                                    <p class="card-text">
                                        DeadLine: ${task.endDate}
                                        <span class="text-info">${task.status}</span>
                                    </p>
                                </div>
                            </div>
                        </div>
                    `;
                        tasksContainer.append(taskCard);
                    });
                } else {
                    tasksContainer.html('<p class="text-center">No tasks found.</p>');
                }
            },
                    error: function () {
                        toastr.error('Failed to update task. Please try again.', 'Error');
                    }
                });

            }
    </script>
}